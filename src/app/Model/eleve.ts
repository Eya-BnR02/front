export class EleveComponent {
    eleve = {
      
      firstname: '', // Initialize to empty string instead of null
      lastname: '',  // Initialize to empty string instead of null
      niveau: ''     // Initialize to empty string instead of null
    };
  
    // ... other component methods ...
  }
  